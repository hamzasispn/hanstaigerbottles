<div class="pt-[10vw] product-info-section" id="product-info-{{ section.id }}">
  <div class="flex flex-col items-center">
    <div class="mb-[1.528vw]">
      {% render 'x1-icon' %}
    </div>
    <span class="text-black text-[0.972vw] leading-[1.389vw]"> From the X1</span>
    <div class="h-[25.417vw] w-full relative">
      <div class="w-full h-full flex flex-col items-center justify-end absolute bottom-0 left-1/2 transform -translate-x-1/2">
        <h2
          id="pi-heading-left-{{ section.id }}"
          class="font-light text-(--color-primary) text-center text-[8.333vw] leading-[7.5vw]"
        >
          {{ section.settings.product_info_title_first_line }}
        </h2>
        <h2
          id="pi-heading-right-{{ section.id }}"
          class="font-light text-(--color-primary) text-center text-[8.333vw] leading-[7.5vw]"
        >
          {{ section.settings.product_info_title_second_line }}
        </h2>
      </div>
      <img
        id="pi-bottle-{{ section.id }}"
        width="400"
        height="400"
        src="{% if section.settings.product_info_image %}{{ section.settings.product_info_image | img_url: 'master' }}{% else %}{{ 'x1-product.png' | asset_url }}{% endif %}"
        alt="Hanstaiger X1 Bottle"
        class="w-[38.056vw] block h-[24.931vw] object-contain absolute -bottom-[4vw] left-1/2 transform -translate-x-1/2 z-10"
      >
    </div>
  </div>
  <div class="pt-[15.139vw] flex items-end justify-between max-w-[58.889vw] mx-auto">
    <div class="w-[35%]">
      <p data-anim="fade-up" class="text-[#83868E] text-[1.111vw] leading-[1.528vw] mb-[2.5vw]">{{ section.settings.product_info_text }}</p>
      <p data-anim="fade-up" class="text-[#222222] font-medium text-[1.111vw] leading-[1.528vw]">
        For those who don’t settle for standard.
      </p>
    </div>
    <div class="w-[65%]">
      {% render 'text-box', secondTextline: section.settings.second_text, blocks: section.blocks %}
    </div>
  </div>
</div>

<script>
(function() {
  var suffix = '{{ section.id }}';
  var bottle   = document.getElementById('pi-bottle-'       + suffix);
  var headLeft = document.getElementById('pi-heading-left-' + suffix);
  var headRight= document.getElementById('pi-heading-right-'+ suffix);
  var section  = document.getElementById('product-info-'    + suffix);

  if (!bottle || !headLeft || !headRight || !section) return;

  // theme.js (defer) exposes window.gsap + window.ScrollTrigger — wait for it
  function setup() {
    if (!window.gsap || !window.ScrollTrigger) { setTimeout(setup, 50); return; }

    var tl = window.gsap.timeline({
      scrollTrigger: {
        trigger: section,
        start: 'top 90%',
        end:   'top 10%',
        scrub: 1.2,
      }
    });

    tl.fromTo(bottle,   { y: 160, opacity: 0 }, { y: 0, opacity: 1, ease: 'none' }, 0)
      .fromTo(headLeft, { x: -180, opacity: 0 }, { x: 0, opacity: 1, ease: 'none' }, 0)
      .fromTo(headRight,{ x:  180, opacity: 0 }, { x: 0, opacity: 1, ease: 'none' }, 0);
  }

  setup();
})();
</script>

{% schema %}
{
  "name": "Product Info",
  "settings": [
    {
      "type": "text",
      "id": "product_info_title_first_line",
      "label": "Product Info Title - First Line",
      "default": "Developed"
    },
    {
      "type": "text",
      "id": "product_info_title_second_line",
      "label": "Product Info Title - Second Line",
      "default": "to endure"
    },
    {
      "type": "text",
      "id": "product_info_text",
      "label": "Product Info Text",
      "default": "Every product in Hanstaiger World was tested aboard the X1 trimaran during extended voyages across the Mediterranean and Atlantic."
    },
    {
      "type": "image_picker",
      "id": "product_info_image",
      "label": "Product Info Image"
    },
    {
      "type": "text",
      "id": "second_text",
      "label": "Second Text Line"
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        {
          "type": "text",
          "id": "feature_text",
          "label": "Feature Text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product Info",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "feature_text": "Engineering"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_text": "Durability"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_text": "BPA-Free Lid"
          }
        },
        {
          "type": "feature",
          "settings": {
            "feature_text": "Performance"
          }
        }
      ]
    }
  ]
}
{% endschema %}
